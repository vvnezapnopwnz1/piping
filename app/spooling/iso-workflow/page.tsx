import { NotOnSupabaseYet } from "@/components/pipeqc/not-on-supabase-yet"

export default function IsoWorkflowPage() {
  return (
    <NotOnSupabaseYet
      title="ISO Workflow"
      track="Track 04 (remaining)"
      summary="Receive, check, hold, release and dispatch isometrics to fabrication. The engineering definitions and revisions behind it are live; the document workflow over them is the part of Track 04 still outstanding."
    />
  )
}
