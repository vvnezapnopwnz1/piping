le секрет закоммичен. docs/TRACK02_BROWSER_FIXTURES.md — строка 11 предписывает «never commit keys to repository files», а строки 26-38 содержат реальный <local-service-role-secret> и
Сводный разбор Track 01 и Track 02

Общий вердикт

Архитектурное ядро сделано грамотно: capability-модель целиком в БД, легаси-роль полностью выведена из авторизации (0 совпадений по pg_policies/pg_proc), все 20+ SECURITY DEFINER функций с закреплённым search_path, ни одной политики USING (true), audit-trail write-only и tamper-resistant, доменный слой modules/project-setup/domain не импортирует ни Supabase, ни React, ни store/\*.

Но зелёный CI не является доказательством выполнения exit-gate. 153 pgTAP + 24 unit-файла проходят — и при этом не покрывают ровно то, что сломано. Ни один из шести блокеров не ловится текущими тестами.

---

Track 01 — блокеры безопасности (подтверждены)

🔴 A1. Захват проекта через самостоятельно редактируемый profiles.email

- 20260727145210:655 → grant update (full_name, email) on public.profiles to authenticated;
- profiles.email — nullable text, без unique-индекса (в таблице только PK по id, строки 38-45)
- 20260731092000:191-193 → invite резолвится по lower(profile.email) = lower(trim(target_email))

Любой аутентифицированный пользователь без единого membership делает update public.profiles set email='<чужой адрес>' (RLS сама ограничит строку) и подменяет свою идентичность. Дальше Project Admin приглашает этот адрес → membership достаётся атакующему. Ревьюер воспроизвёл на живой БД: add_project_member_by_email вернул access_role_code = project_admin, current_user_has_capability(..., 'access_rights.manage') → t. Полный захват администрирования проекта.

Побочный вектор: если жертва уже зарегистрирована — дубликат email даёт cardinality <> 1 → PQC01 навсегда, то есть постоянный DoS на приглашение конкретного человека.

Фикс: резолвить приглашение по auth.users.email внутри SECURITY DEFINER; revoke update (email) on public.profiles from authenticated; create unique index on public.profiles (lower(email)).

🔴 A2. TRUNCATE/TRIGGER не отозваны на access-таблицах

20260731092000:278 отзывает только insert, update, delete. Supabase раздаёт default ALL на новые таблицы в public, поэтому у anon и authenticated остались TRUNCATE, TRIGGER, REFERENCES на proje_subcontractor_scopes,membership_pds_area_scopes, projects, profiles и др. Ревьюер выполнил truncate public.membership_subcontractor_scopes от имени обычного project_reader — успешно. TRUNCATE полностью игнорирует RLS.
Честная оговорка: через Postостижимо (нет такогоглагола), так что из браузера не эксплуатируется. Но привилегия существует, а те011:195-198 создаёт впечатлерыт.
Фикс: паттерн revoke all ... from anon, authenticated + явные точечные гранты — он уже правильно применён к roles/capabilities/audit_events, просто не доведён до access-таблиц.

--- Track 02 — функциональные бл

🔴 B1. Cross-project guard-триггеры — no-op  
20260801091000:269-287 — четыре триггера вызывают assert*same_project_reference() без аргументов, а функция построена на tg_argv[0]/tg_argv[1]. Ранний return new. Cross-project вставки проходят (воспроизведено). Exit-критерий, ради которого написана вся миграция, не выполнен; тест 021 зелёный, потому что проверяет FK, защищённый старым триггером
🔴 B2. set_project_progress*сохранении

Архивирует строки без удаления, затем вставляет те же (project_id, phase, activiпри unique без учёта статусаое редактирование весовпосле первого Save → duplicate key.
🔴 B3. Логотипы на Storage: несуществующие колонки

supabase-project-branding.ts:56 пишет owner_logo_url, в схеме — owner_logo_path (20260727145210:76-77); as a; адаптер не импортируется вручную обходным способом. То есть «все проверки прошли» в handoff-доках нельзя перепроверить одной командой.

Ещё один факт по exit-критер query не используетlocalStorage subcontractor code»: lib/scope-lock.ts:5,11,19 по-прежнему импортирует
useAdminStore и читает windopeqc-active-sub"). ВеткаappMode === "supabase" (строка 31) эти значения не использует, но хук считает их
безусловно — насколько это путочнит ревьюер T1.

Жду отчёты обоих ревьюеров —с приоритетами.

✻ Brewed for 3m 24s

⏺ Agent "Review Track 02 referentials" finished · 35m 54s

Отчёт по Track 02 пришёл. Проверяю сам два самых тяжёлых утверждения, прежде чем
передавать их как факт.

Ran 5 shell commands

Отчёт по Track 02 готов; я н Critical-находки поисходникам — все четыре подтверждаются. Ревьюер по Track 01 ещё работает.

Track 02 — подтверждённые бл
├───────────────────────────────┼──────────────────────────────────────────────┤
│ Отдельный план 2026-07-31-tra │ файла нет │
│ ck-00-foundation.md │
└───────────────────────────────┴──────────────────────────────────────────────┘

Два следствия, важных именно для оценки T1/T2:

1. Вся авторизация — client-Client|@supabase/ssr даётноль совпадений, app/admin/\*/page.tsx — "use client". Значит
   route-capability-guard.tsx э настоящий гейт — RLS.Roadmap-критерий T1 «скрытый route и прямой URL дают одинаковую capability
   semantics» держится исключитик — это ревьюер сейчаспроверяет предметно.
2. Проверки, на которые ссылаются планы, не воспроизводимы командой из плана.
   Требуемые в T0 npm run test: существуют; я прогонял тесты вручную обходным способом. То есть «все проверки прошли» в handoff-доках нельзя перепроверить одной командой.

Ещё один факт по exit-критер query не используетlocalStorage subcontractor code»: lib/scope-lock.ts:5,11,19 по-прежнему импортирует
useAdminStore и читает windopeqc-active-sub"). ВеткаappMode === "supabase" (строка 31) эти значения не использует, но хук считает их
безусловно — насколько это путочнит ревьюер T1.

Жду отчёты обоих ревьюеров — сведу их в единый разбор с приоритетами.

✻ Brewed for 3m 24s

⏺ Agent "Review Track 02 referentials" finished · 35m 54s

Отчёт по Track 02 пришёл. Прых утверждения, прежде чемпередавать их как факт.

Ran 5 shell commands

Отчёт по Track 02 готов; я н Critical-находки поисходникам — все четыре подтверждаются. Ревьюер по Track 01 ещё работает.

Track 02 — подтверждённые бл
ecture/context-map │ │
│ .md, domain-glossary.md, ADR │ нет каталога docs/architecture/ │
│ 0001–0004 │ │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ modules/shared/domain/result. │ │
│ ts, errors.ts, │ нет каталога modules/shared/ │
│ application/command.ts, │ │
│ repository.ts │ │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ @supabase/ssr, split │ не установлен; есть только │
│ browser/server clients │ lib/supabase/browser-client.ts │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ proxy.ts / middleware с │ нет ни middleware.ts, ни proxy.ts │
│ cookie session refresh │ │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ ESLint работает │ eslint не установлен, npm run lint падает с │
│ │ command not found │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ Vitest / Playwright configs │ нет │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ npm run verify, typecheck, │ в package.json только dev/build/start/lint/v │ test:unit, test:db, test:etstrap:\* │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ Отдельный план 2026-07-31-tra │ файла нет │ ck-00-foundation.md │ржится исключительно на полноте RLS-политик — это ревьюер сейчас проверяет предметно. 2. Проверки, на которые ссылаются планы, не воспроизводимы командой из плана. Требуемые в T0 npm run test:unit / test:db / verify не существуют; я прогонял тесты вручную обходным способом. То есть «все проверки прошли» в handoff-доках нельзя перепроверить одной командой.

Ещё один факт по exit-критерию T1 «ни один operational query не использует localStorage subcontractor code»: lib/scope-lock.ts:5,11,19 по-прежнему импортирует useAdminStore и читает window.localStorage.getItem("pipeqc-active-sub"). Ветка appMode === "supabase" (строка 31) эти значения не использует, но хук считает их безусловно — насколько это протекает в Supabase mode, уточнит ревьюер T1.

Жду отчёты обоих ревьюеров — сведу их в единый разбор с приоритетами.

✻ Brewed for 3m 24s

⏺ Agent "Review Track 02 referentials" finished · 35m 54s

Отчёт по Track 02 пришёл. Проверяю сам два самых тяжёлых утверждения, прежде чем передавать их как факт.  
 Ran 5 shell commands
Отчёт по Track 02 готов; я н Critical-находки поисходникам — все четыре подтверждаются. Ревьюер по Track 01 ещё работает.
Track 02 — подтверждённые блodes.line_service_id, project_paint_matrix_rules.line_service_id и .ral_code_id, project_spooling_material_classes.material_type_id. Ревьюер воспроизвёл вставку cross-project строк на живой БД. Это ровно тот exit-критерий, ради которого писалась миграция 091000. Тест 021 даёт ложное «зелёное», потому что проверяет только shop_subcontractor_id, защищённый старым триггером из Track 01.

2. set_project_progress_weights ломается на втором сохранении. RPC (20260801091000:351-357) переводит старые строки в status='archived', но не удаляет их, и тут же вставляет новые с теми же activity. На таблице — unique (project_id, phase, activity) без учёта статуса (20260727145210:427). Итог: любое редактирование весов после первого Save падает с duplicate key. Тест 021:239-246 делает ровно один вызов, поэтому не ловит.

3. Логотипы на Storage не подключены, адаптер пишет в несуществующие колонки. modules/project-setup/infrastructure/supabase-project-branding.ts:56 пишет owner_logo_url/contractor_logo_url, а в схеме — owner_logo_path/contractor_logo_path (20260727145210:76-77). as any на строке 59 глушит tsc. Проверил grep — адаптер не импортируется нигде, форма Project Definition до сих пор на текстовых URL-полях. При этом docs/SUPABASE_BACKEND_FOUNDATION.md утверждает, что логотипы уже в приватном бакете.
4. Service-role секрет закоммичен. docs/TRACK02_BROWSER_FIXTURES.md — строка 11 предписывает «never commit keys to repository files», а строки 26-38 содержат реальный sb_secret_N7UND0UgjTRACK01_FIXTURE_PASSWORD='Password123!'. Файл untracked → уйдёт в первый же коммит. Ключ локальный, но перевыпустить и почистить нужно.  
   политики не удалены. Список drop policy if exists в 20260801091000:154-166 перечисляет несуществующие имена; реальные — <t>\_members_read / <t>\_admins_write — живы на ~25 таблицах и permissive (OR-семантика). \_admins_write это FOR ALL, то есть включает SELECT и обходит новые scope-фильтры по subcontractor/PDS. Сегодня ущерб нулевой, но заявленный capability-гейт декоративен.

- Сумма 100 не защищена на уровне таблицы — authenticated имеет прямые INSERT/UPDATE-гранты, инвариант живёт только внутри RPC.
- Часть T2-задач сделана как read-only схема, а не CRUD: Spooling Material Type/Class/Checklist, RAL + Paint Matrix, PDA Users, UI определений custom fields. Мутаций нет, UI рисует статичные списки.
- Archive-in-use и «список blockers» отсутствуют: grep по миграциям на blocker|in_use|usage даёт ноль. Удалить используемый reference нельзя — но только потому, что нельзя удалить никакой (нет DELETE-гранта).
- Storage-политики не протестированы вовсе: тест 022 проверяет только метаданные бакета (public=false, лимит размера). Вопрос «читает ли проект A логотип проекта B» не доказан. Плюс политики созданы без TO authenticated → применяются к PUBLIC.
- Bootstrap-фикстуры — заглушка: scripts/bootstrap-track02-browser-fixtures.ts — 77 строк, 0 вызовов .insert/.upsert (для сравнения Track 01 — 292 строки). Печатает «completed successfully», ничего не вставив. Task 16 не выполнен, а рунбук описывает реконсиляцию, которой нет. - Unit-тесты mock-shaped: феdata: [], error: null} навсё, проверяется только queries.includes("from:..."). Ни один тест не проверяет имена колонок — поэтому баг №3 прошёл 24 зелёных теста и tsc.  
  Что сделано хорошо
  Доменный слой чистый — modulне импортирует ни Supabase,блицами с CHECK-ограничениями, а не jsonb — запрет плана соблюдён. Все 7 новых SECURITY DEFINER-функций имеют закреплённый search_path, политик USING (true) нет. Audit-триггеры на 31 таблице. Setup readiness — реальные exists-запросы, а не хардкод, с корректным учётом is_assembly_enabled.

Главный вывод по процессу

Документация опережает код: docs/SUPABASE_NEXT_AGENT_CONTEXT.md заявляет «Track 02 is 100% complete», SUPABASE_BACKEND_FOUNDATION.md — что 153 pgTAP-assertions проверяют «cross-project relationship protection» и «private logo storage policies». Ни то, ни другое не проверяется. Сам план 2026-08-01-track-02-...md при этом честен — в нём только - [ ], ложных отметок нет.

Вердикт по T2: к T3 не готов. Архитектурную основу переписывать не нужно, но перед T3 надо закрыть 4 блокера, вычистить легаси-политики и заменить mock-shaped тесты на поведенческие — иначе следующий трек унаследует зелёный CI поверх неработающих инвариантов.

Жду отчёт по Track 01, потом сведу общий приоритизированный список.

✻ Brewed for 1m 17s  
⏺ Agent "Review Track 01 access" finished · 38m 51s
Проверяю Critical-находки по

Ran 2 shell commands
Оба ревью получены. Все Critл по исходникам —подтверждаются все шесть.

---

Сводный разбор Track 01 и Track 02
Общий вердикт

Архитектурное ядро сделано грамотно: capability-модель целиком в БД, легаси-ролполностью выведена из авториpolicies/pg*proc), все 20+SECURITY DEFINER функций с закреплённым search_path, ни одной политики USING (true), audit-trail write-onенный слойmodules/project-setup/domain не импортирует ни Supabase, ни React, ни store/*.
Но зелёный CI не является доказательством выполнения exit-gate. 153 pgTAP + 24 unit-файла проходят — и при этом не покрывают ровно то, что сломано. Ни один изшести блокеров не ловится те
middleware с │ нет ни middleware.ts, ни proxy.ts │
│ cookie session refresh │ │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ ESLint работает │ eslint не установлен, npm run lint падает с │
│ │ command not found │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ Vitest / Playwright configs │ нет │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ npm run verify, typecheck, │ в package.json только dev/build/start/lint/v │
│ test:unit, test:db, test:e2e │ alidate:fixtures/bootstrap:\_ │
├───────────────────────────────┼──────────────────────────────────────────────┤
│ Отдельный план 2026-07-31-tra │ файла нет │
│ ck-00-foundation.md │ │
└───────────────────────────────┴──────────────────────────────────────────────┘

Два следствия, важных именно для оценки T1/T2:

1. Вся авторизация — client-side. grep по createServerClient|@supabase/ssr даёт ноль совпадений, app/admin/\*/page.tsx — "use client". Значит route-capability-guard.tsx это UX-слой, а единственный настоящий гейт — RLS. Roadmap-критерий T1 «скрытый route и прямой URL дают одинаковую capability semantics» держится исключитик — это ревьюер сейчаспроверяет предметно.
2. Проверки, на которые ссылаются планы, не воспроизводимы командой из плана. Требуемые в T0 npm run test: существуют; я прогонял тесты вручную обходным способом. То есть «все проверки прошли» в handoff-доках нельзя перепроверить одной командой.  
   Ещё один факт по exit-критерию T1 «ни один operational query не использует localStorage subcontractor code»: lib/scope-lock.ts:5,11,19 по-прежнему импортирует useAdminStore и читает window.localStorage.getItem("pipeqc-active-sub"). Ветка appMode === "supabase" (строка 31) эти значения не использует, но хук считает их безусловно — насколько это протекает в Supabase mode, уточнит ревьюер T1.  
   Жду отчёты обоих ревьюеров — сведу их в единый разбор с приоритетами.
   Brewed for 3m 24s

⏺ Agent "Review Track 02 referentials" finished · 35m 54s  
Отчёт по Track 02 пришёл. Проверяю сам два самых тяжёлых утверждения, прежде чем передавать их как факт.  
 Ran 5 shell commands  
Отчёт по Track 02 готов; я независимо перепроверил все Critical-находки по исходникам — все четыре подтверждаются. Ревьюер по Track 01 ещё работает.  
Track 02 — подтверждённые блокеры

1. Cross-project guard-триггеры — полный no-op. supabase/migrations/20260801091000_referential_invariants.sql:269-287 — четыре триггера вызывают execute fuproject_reference() безаргументов, а функция (20260727145210:469-491) целиком построена на tg_argv[0]/tg_argv[1]. При tnb(new) ->> NULL → NULL →ранний return new. Не защищены project_pds_areas.assembly_subcontractor_id, project_ral_codes.line_servix_rules.line_service_id и.ral_code_id, project_spooling_material_classes.material_type_id. Ревьюер воспроизвёл вставку cross-prто ровно тот exit-критерий,ради которого писалась миграция 091000. Тест 021 даёт ложное «зелёное», потому что проверяет только shop_subcontractor_id, защищённый старым триггером из Track 01
2. set_project_progress_weights ломается на втором сохранении. RPC (20260801091000:351-357) переводит старые строки в status='archived', но не удаих, и тут же вставляет новыеблице — unique (project_id,phase, activity) без учёта статуса (20260727145210:427). Итог: любое редактирование весов после первого Save падает с duplicate key. Тест 021:239-246 делает ровно один вызов, поэтому не ловит

3. Логотипы на Storage не подключены, адаптер пишет в несуществующие колонки. modules/project-setup/infrasbranding.ts:56 пишетowner_logo_url/contractor_logo_url, а в схеме — owner_logo_path/contractor_logo_path (20260727145210:76-77). as any на строке 5глушит tsc. Проверил grep — игде, форма ProjectDefinition до сих пор на текстовых URL-полях. При этом docs/SUPABASE_BACKEND_FOUNDAготипы уже в приватномбакете. 4. Service-role секрет закомR_FIXTURES.md — строка 11предписывает «never commit keys to repository files», а строки 26-38 содержат реальный <local-service-role-secret> и TRACK01_FIXTURE_PASSWORD='Pad → уйдёт в первый же коммит. Ключ локальный, но перевыпустить и почистить нужно.

Существенное, но не блокирующее - Легаси RLS-политики не удаf exists в20260801091000:154-166 перечисляет несуществующие имена; реальные — <t>\_members_read / <t>\_adminицах и permissive(OR-семантика). \_admins_write это FOR ALL, то есть включает SELECT и обходит ноscope-фильтры по subcontractвой, но заявленныйcapability-гейт декоративен.

- Сумма 100 не защищена на уровне таблицы — authenticated имеет прямые INSERT/UPDATE-гранты, инвариC.
- Часть T2-задач сделана как read-only схема, а не CRUD: Spooling Material Type/Class/Checklist, RAL + Paint Matrix, PDA Users, UI определений custom fields. Мутаций нет, UI рисует статичные списки.
- Archive-in-use и «список blockers» отсутствуют: grep по миграциям на blocker|in_use|usage даёт ноeference нельзя — но толькопотому, что нельзя удалить никакой (нет DELETE-гранта). - Storage-политики не протесроверяет только метаданныебакета (public=false, лимит размера). Вопрос «читает ли проект A логотип проектне доказан. Плюс политики со → применяются к PUBLIC.
- Bootstrap-фикстуры — заглушка: scripts/bootstrap-track02-browser-fixtures.ts — 77 строк, 0 вызовов .insert/.upsert (для сравнения Track 01 — 292 строки). Печатае«completed successfully», ние выполнен, а рунбукописывает реконсиляцию, которой нет.
- Unit-тесты mock-shaped: фейковый клиент возвращает {data: [], error: null} навсё, проверяется только querНи один тест не проверяетимена колонок — поэтому баг №3 прошёл 24 зелёных теста и tsc.
  Что сделано хорошо

Доменный слой чистый — modules/project-setup/domain/_ не импортирует ни Supabasни React, ни store/_. Typed ty и UT Calculation сделанынастоящими таблицами с CHECK-ограничениями, а не jsonb — запрет плана соблюдён. Все 7 новых SECURITY DEFINER-функций имеют закреплённый search_path, политик USING (true) нет. Audit-триггеры нess — реальныеexists-запросы, а не хардкод, с корректным учётом is_assembly_enabled.
Главный вывод по процессу

Документация опережает код: docs/SUPABASE_NEXT_AGENT_CONTEXT.md заявляет «Track 02 is 100% complete», SUPABASE_BACKEND_FOUNDATION.md — что 153 pgTAP-assertions проверяют «cross-project relrivate logo storagepolicies». Ни то, ни другое не проверяется. Сам план 2026-08-01-track-02-...md этом честен — в нём только -

Вердикт по T2: к T3 не готов. Архитектурную основу переписывать не нужно, но пеT3 надо закрыть 4 блокера, в заменить mock-shaped тестына поведенческие — иначе следующий трек унаследует зелёный CI поверх неработающих инвариантов.  
Жду отчёт по Track 01, потом сведу общий приоритизированный список.
Brewed for 1m 17s

⏺ Agent "Review Track 01 access" finished · 38m 51s  
Проверяю Critical-находки по Track 01 независимо.  
 Ran 2 shell commands  
Оба ревью получены. Все Critical-находки я перепроверил по исходникам — подтверждаются все шесть.

--- Сводный разбор Track 01 и Tr

Общий вердикт
Архитектурное ядро сделано г целиком в БД, легаси-рольполностью выведена из авторизации (0 совпадений по pg_policies/pg_proc), все 20SECURITY DEFINER функций с зи одной политики USING(true), audit-trail write-only и tamper-resistant, доменный слой modules/project-setup/domaine, ни React, ни store/\*.

Но зелёный CI не является доказательством выполнения exit-gate. 153 pgTAP + 24 unit-файла проходят — и при , что сломано. Ни один изшести блокеров не ловится текущими тестами.

---

Track 01 — блокеры безопасности (подтверждены)  
🔴 A1. Захват проекта через самостоятельно редактируемый profiles.email

- 20260727145210:655 → grant update (full_name, email) on public.profiles to authenticated;
- profiles.email — nullable text, без unique-индекса (в таблице только PK по id, строки 38-45) - 20260731092000:191-193 → iprofile.email) =lower(trim(target_email))

Любой аутентифицированный пользователь без единого membership делает update public.profiles set email='<чужой адрес>' (RLS сама ограничит строку) и подменясвою идентичность. Дальше Prт адрес → membershipдостаётся атакующему. Ревьюер воспроизвёл на живой БД: add_project_member_by_emвернул access_role_code = prhas_capability(...,'access_rights.manage') → t. Полный захват администрирования проекта.
Побочный вектор: если жертвабликат email даёт cardinality <> 1 → PQC01 навсегда, то есть постоянный DoS на приглашение конкретного человека.
Фикс: резолвить приглашение SECURITY DEFINER; revokeupdate (email) on public.profiles from authenticated; create unique index on public.profiles (lower(email)).  
🔴 A2. TRUNCATE/TRIGGER не отозваны на access-таблицах

20260731092000:278 отзывает только insert, update, delete. Supabase раздаёт default ALL на новые таблицы в public, поэтому у anon и authenticated остались TRUNCATETRIGGER, REFERENCES на proje_subcontractor_scopes,membership_pds_area_scopes, projects, profiles и др. Ревьюер выполнил truncate public.membership_subcontracго project_reader — успешно.TRUNCATE полностью игнорирует RLS.  
Честная оговорка: через PostgREST HTTP это сегодня недостижимо (нет такого глагола), так что из браузера не эксплуатируется. Но привилегия существует, а т011:195-198 создаёт впечатлерыт.

## Фикс: паттерн revoke all ... from anon, authenticated + явные точечные гранты —уже правильно применён к rolts, просто не доведён доaccess-таблиц.

Track 02 — функциональные блокеры (подтверждены)
🔴 B1. Cross-project guard-т

20260801091000:269-287 — четыре триггера вызывают assert_same_project_referenceбез аргументов, а функция поrgv[1]. Ранний return new.Cross-project вставки проходят (воспроизведено). Exit-критерий, ради которого написана вся миграция, не выполнен; тест 021 зелёный, потому что проверяет FK, защищённый старым триггером

🔴 B2. set_project_progress_weights падает при втором сохранении

Архивирует строки без удаления, затем вставляет те же (project_id, phase, activity) при unique без учёта статуса (20260727145210:427). Любое редактирование весов после первого Save → duplicate key.

🔴 B3. Логотипы на Storage: адаптер мёртвый и пишет в несуществующие колонки

supabase-project-branding.ts:56 пишет owner_logo_url, в схеме — owner_logo_path (20260727145210:76-77); as any на строке 59 глушит tsc; адаптер не импортируется нигде (проверено grep). Форма Project Definition по-прежнему на текстовых URL. Документация утверждает обратное.

🔴 B4. Service-role секрет в репозитории

## docs/TRACK02_BROWSER_FIXTURES.md — строка 11 запрещает коммитить ключи, строки 26-38 содержат реальный sb_sE_PASSWORD. Файл untracked →уйдёт в первый коммит.

Расхождения с dossier (требуют вашего решения, не фикса)
Subcontractor scope сделан mubcontractor». Dossier(строки 224-233) прямо говорит: выбирается один subcontractor, поле Subcontractor во всех экранах disabled и принудительно равно logged-in. Реализовано как membership_subcontractor_scoией cardinality >= 1. Второетребование не реализовано вообще. Если правило «один» верно — это инвариант БД, и добавить его позже будет migration-дорого.  
Platform admin не видит неактивные проекты. 20260731091000:164-166 фильтрует status='active' даже для platform admin, а current_user_has_capability отдаёт fна любую мутирующую capabiliadmap требует «System Adminвидит все проекты», dossier §6.4 — «удалить project может System Admin из другоproject». Архивный проект се
Test Pack functional role неabilities выданыqc_engineer/nde_inspector (catalog:130-137), хотя roadmap перечисляет Test Pack как отдельную функциональную роль.

---

Системные проблемы процесса 1. T0 пропущен целиком. Нет xt-map, glossary, 4 ADR), нет modules/shared/\* (Result, errors, command port), нет @supabase/ssr, нет middleware.ts/proxy.ts, ESLilint падает), нетvitest/playwright, нет плана track-00-foundation.md. T1 и T2 построены поверх отсутствующего фундамента.

Прямое следствие: вся авторизация client-side. route-capability-guard.tsx смонтирован в клиентском Appout.tsx не создан. 32страницы — Server Components, их содержимое уезжает в RSC-payload до решения guard'а. Сегодня утечки нет (страницы статические), но первый же серверный fetch на защищённой странице её создаст. 2. Тесты создают ложную уверpe_isolation.test.sqlфикстура inactive@example.test и membership с is_active=false создаются и никогне используются — сессии дляатрицы «пользователь безmembership» не проверяется ни разу. Reader × Mutation и Subcontractor × Mutatioпроверяются только булевым c, без единой реальной попытки UPDATE. Unit-тесты в modules/project-setup/infrastructure — mock-shaped: фейковый клиент возвращает {data: [], error: null} на всё, проверяется только queries.includes("from:...")т имена колонок — поэтому B3прошёл 24 зелёных теста и tsc. 3. Документация опережает коTEXT.md: «Track 02 is 100%complete». SUPABASE_BACKEND_FOUNDATION.md: 153 assertions проверяют «cross-project relationship protection» и «private logo storage policies» — не проверяется ни ни другое (тест 022 смотрит Сами планы при этом честны:в 2026-08-01-track-02-\*.md только - [ ]. 4. Проверки невоспроизводимыheck, test:unit, test:db,verify, test:e2e, на которые ссылаются планы. Я запускал всё ad-hoc обходными командами. Следующий агент не сможет повторить «все проверки прошли» одной командой.

--- Приоритет действий

Hotfix-набор миграций перед любой работой над T3:

1. A1 — резолв invite через auth.users.email + отзыв update(email) + unique-индекс
2. A2 — revoke all на access-таблицах, затем точечные гранты
3. B1 — передать tg_argv в 5 триггеров (по одному на FK-колонку)
4. B2 — delete вместо archive внутри RPC либо on conflict do update
5. B4 — вычистить секрет из рунбука, перевыпустить локальный ключ
   Затем, до T3:
6. B3 — исправить колонки, убрать as any, подключить upload в форму
7. Вычистить легаси permissive-политики <t>\_members_read/<t>\_admins_write (списdrop policy в 20260801091000ествующие имена — реальныеживы на ~25 таблицах и обходят capability-гейт)
8. Заменить mock-shaped тесты на поведенческие: cross-project FK, повторное сохранение весов, Storage-пореальные мутации отReader/Subcontractor
   Отдельно — восстановить T0, к будет наследоватьclient-only авторизацию и невоспроизводимые проверки.
   Можно вынести в T2.1 (но тогентации): отсутствующий CRUDдля Spooling Material/RAL/Paint Matrix/PDA Users/custom fields (сейчас read-only схема), archive-in-use со списком blockers (grep по миграциям на blocker|in_use → 0), заглушка bootstrap-track02-browser-fixtures.ts (77 строк, 0 вызовов .insert
   Ещё одна находка на стыке треков: после 20260801093000 у system_reference_entries нет ни одной PERMISSIVE DELETE-политики (только три RESTRICTIVE), при выданном grant delete — удаление systвообще ни для кого, включаяplatform admin.

---

Могу подготовить hotfix-план по пунктам 1-5 в формате исполняемого плана, если нужно.
